# Implement customer health score calculation engine

Built a health score engine that aggregates login frequency, feature adoption, support ticket volume, and NPS scores into a 0-100 health score per customer. Runs daily via cron job. Results stored in `customer_health` table.
